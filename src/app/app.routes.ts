import { Routes } from '@angular/router';

export const routes: Routes = [];



// Declares an (empty) router config. If you plan to add lazy routes or navigation, populate this array. Right now it is unused (no router in AppComponent) — fine for a single-page anchored portfolio.